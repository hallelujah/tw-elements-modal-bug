# Issue with TailwindElements Modal

Using `modal.dispose()` does not clear the event listener on `EVENT_KEYDOWN_DISMISS`

## See it live

```shell
npm install
npm start
```

- Visit http://localhost:3000 in your browser
- Follow the instructions on the screen
